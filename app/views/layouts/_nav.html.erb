<% if current_user %>
<nav class="navbar navbar-expand-lg bg-darki">
  <div class="container-fluid">
    <div class="navbar-brand">
        <%= link_to "Back", categories_path, class: "back" %> 
    </div>
     <div class="logo">Savier</div>
  
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
   
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
         <%= link_to "All categories", categories_path, class: "nav-item nav-link" %>
           <%= link_to "New category", new_category_path, class: "nav-item nav-link" %>

         <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "nav-logout nav-link" %>
          <li class="prof">
          <% if current_user.avatar.attached? %> 
          <%= image_tag current_user.avatar, class: "nav-link avatar"%>
           <% else %>
            <%= image_tag "avatar.png", class: "avatar" %>
           <% end %>
           <%= link_to current_user.name, categories_path, class: "nav-item nav-link" %>
          
        </li>
      </ul>
    </div>
  </div>
</nav>
<% else %>
<nav class="navbar navbar-expand-lg bg-darki">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <%= image_tag "no-profile.jpg", class: "avatar"%>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle
                            navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <li><a class="nav-link" href="/users/sign_in">Login </a></li>
            <li><a class="nav-link" href="/users/sign_up">sign up</a></li>
         
        </li>
      </ul>
    </div>
  </div>
</nav>
<% end %>
